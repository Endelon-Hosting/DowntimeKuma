@using S3
@inject IS3Service s3

@if(s3.HasSession)
{
    @if(s3.Storage.ContainsKey("auth"))
    {
        @if(s3.Storage.Get<bool>("auth"))
        {
            @ChildContent
        }
    }
}

@code
{
    [Parameter]
    public RenderFragment ChildContent { get; set; }
}